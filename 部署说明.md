# 前端部署说明

## 打包完成

前端项目已成功打包，生成的文件位于 `web-ui/dist` 目录。

### 打包文件清单

```
web-ui/dist/
├── index.html          (452 bytes)
├── vite.svg           (1.5 KB)
└── assets/
    ├── index-DQeXyWkx.css    (356 KB, gzip: 49 KB)
    └── index-C0r6yji9.js     (1.2 MB, gzip: 387 KB)
```

## Nginx 部署方案

### 1. 配置文件示例

创建 Nginx 配置文件 `/etc/nginx/sites-available/oncall-system`：

```nginx
server {
    listen 80;
    server_name your-domain.com;  # 替换为您的域名或IP

    # 前端静态文件
    location / {
        root /var/www/oncall-system;  # 替换为实际路径
        try_files $uri $uri/ /index.html;
        index index.html;
        
        # 启用 gzip 压缩
        gzip on;
        gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
        gzip_min_length 1000;
    }

    # 后端 API 代理
    location /api/ {
        proxy_pass http://localhost:8888/api/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        root /var/www/oncall-system;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

### 2. 部署步骤

#### 步骤 1: 上传文件到服务器

```bash
# 在本地机器上，将 dist 目录打包
cd d:\重构代码\oncallSystem\web-ui
tar -czf dist.tar.gz dist/

# 上传到服务器
scp dist.tar.gz user@your-server:/tmp/

# 在服务器上解压
ssh user@your-server
cd /var/www
sudo mkdir -p oncall-system
cd oncall-system
sudo tar -xzf /tmp/dist.tar.gz --strip-components=1
```

#### 步骤 2: 配置 Nginx

```bash
# 创建配置文件
sudo nano /etc/nginx/sites-available/oncall-system

# 粘贴上面的配置内容，修改 server_name 和路径

# 创建软链接启用配置
sudo ln -s /etc/nginx/sites-available/oncall-system /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重启 Nginx
sudo systemctl restart nginx
```

#### 步骤 3: 部署后端服务

```bash
# 上传后端代码
scp -r server/ user@your-server:/opt/oncall-system/

# 在服务器上编译运行
ssh user@your-server
cd /opt/oncall-system/server
go build -o oncall-server main.go

# 使用 systemd 管理后端服务
sudo nano /etc/systemd/system/oncall-backend.service
```

**systemd 服务配置：**

```ini
[Unit]
Description=OnCall System Backend
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/opt/oncall-system/server
ExecStart=/opt/oncall-system/server/oncall-server
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

```bash
# 启动服务
sudo systemctl daemon-reload
sudo systemctl enable oncall-backend
sudo systemctl start oncall-backend

# 查看状态
sudo systemctl status oncall-backend
```

### 3. HTTPS 配置（可选）

使用 Let's Encrypt 免费证书：

```bash
# 安装 certbot
sudo apt-get install certbot python3-certbot-nginx

# 获取证书并自动配置 Nginx
sudo certbot --nginx -d your-domain.com

# 证书会自动续期
```

## Windows IIS 部署方案（可选）

### 1. 安装 URL Rewrite 模块

从 Microsoft 官网下载并安装 URL Rewrite 模块。

### 2. 配置 web.config

在 `dist` 目录创建 `web.config`：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <system.webServer>
    <rewrite>
      <rules>
        <rule name="API Proxy" stopProcessing="true">
          <match url="^api/(.*)" />
          <action type="Rewrite" url="http://localhost:8888/api/{R:1}" />
        </rule>
        <rule name="SPA Routes" stopProcessing="true">
          <match url=".*" />
          <conditions logicalGrouping="MatchAll">
            <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true" />
            <add input="{REQUEST_FILENAME}" matchType="IsDirectory" negate="true" />
          </conditions>
          <action type="Rewrite" url="/" />
        </rule>
      </rules>
    </rewrite>
    <staticContent>
      <mimeMap fileExtension=".json" mimeType="application/json" />
    </staticContent>
  </system.webServer>
</configuration>
```

### 3. 在 IIS 中创建网站

1. 打开 IIS 管理器
2. 右键"网站" → "添加网站"
3. 网站名称：OnCall System
4. 物理路径：选择 `dist` 目录
5. 端口：80（或其他）
6. 启动网站

## 本地测试部署

### 使用 http-server（简单测试）

```bash
# 安装 http-server
npm install -g http-server

# 在 dist 目录运行
cd web-ui/dist
http-server -p 8080 --proxy http://localhost:8888
```

### 使用 serve（推荐）

```bash
# 安装 serve
npm install -g serve

# 运行
cd web-ui/dist
serve -s . -l 8080
```

注意：本地测试时需要确保后端服务运行在 `http://localhost:8888`

## 环境变量配置

如果需要在不同环境使用不同的 API 地址，可以修改 `web-ui/.env.production`：

```env
VITE_API_BASE_URL=http://your-api-server.com
```

然后在代码中使用：

```typescript
const apiBaseUrl = import.meta.env.VITE_API_BASE_URL || ''
axios.get(`${apiBaseUrl}/api/groups`)
```

## 验证部署

部署完成后，访问以下 URL 验证：

1. **前端页面**：`http://your-domain.com`
2. **Dashboard**：`http://your-domain.com/dashboard`
3. **API 测试**：`http://your-domain.com/api/groups`

## 故障排查

### 前端无法访问

- 检查 Nginx 配置是否正确
- 检查文件权限：`sudo chown -R www-data:www-data /var/www/oncall-system`
- 查看 Nginx 错误日志：`sudo tail -f /var/log/nginx/error.log`

### API 请求失败

- 检查后端服务是否运行：`sudo systemctl status oncall-backend`
- 检查防火墙设置：`sudo ufw status`
- 查看后端日志：`sudo journalctl -u oncall-backend -f`

### 路由 404 错误

- 确保 Nginx 配置了 `try_files $uri $uri/ /index.html`
- 检查 Vue Router 是否使用 history 模式

## 性能优化建议

1. **启用 gzip 压缩**（已在 Nginx 配置中）
2. **配置静态资源缓存**（已在 Nginx 配置中）
3. **使用 CDN** 加速静态资源
4. **启用 HTTP/2**（需要 HTTPS）
5. **代码分割**：考虑使用动态 import() 拆分大文件

## 更新部署

当需要更新前端时：

```bash
# 1. 本地重新打包
cd web-ui
npm run build

# 2. 上传到服务器
scp -r dist/* user@your-server:/var/www/oncall-system/

# 3. 清除浏览器缓存或使用版本号
```

## 备份建议

定期备份以下内容：

1. 数据库文件：`server/oncall.db`
2. Nginx 配置：`/etc/nginx/sites-available/oncall-system`
3. 后端代码和配置

```bash
# 备份脚本示例
#!/bin/bash
DATE=$(date +%Y%m%d_%H%M%S)
tar -czf backup_$DATE.tar.gz \
    /opt/oncall-system/server/oncall.db \
    /etc/nginx/sites-available/oncall-system
```
